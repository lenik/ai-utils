#!/bin/sh
# master-file
# Disable and stop aifmt (binfmt_misc) via systemd.

if command -v systemctl >/dev/null 2>&1; then
    systemctl stop aifmt.service
    systemctl disable aifmt.service
    systemctl daemon-reload
else
    @pkgdatadir@/aifmt stop 2>/dev/null || true
fi

exit 0
